plugins {
    id("java-library")
}

dependencies {
    api(project(":internal:common:annotations"))
    api(project(":internal:common:io")) // TODO: extract this to an interface
    api(project(":core:entities"))
}
